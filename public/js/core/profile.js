var I = Object.defineProperty; var f = (e, t) => I(e, "name", { value: t, configurable: !0 }); import { UI as r } from "./ui.js"; import { DASHBOARD_CONFIG as g } from "../features/dashboard/dashboard-config.js"; import { AccountMessages as y } from "../features/dashboard/account/messages.js"; import { HtmlLoader as h } from "../shared/utils/htmlLoader.js"; import { Loader as w } from "../shared/utils/loader.js"; const C = {
    session: null, isInitialized: !1, rateLimitPollInterval: null, countdown: 30, lastData: { followers: -1, analytics: {} }, get authHeaders() { const t = {}; return this.session?.token && (t.Authorization = `Bearer ${this.session.token}`), t }, get authQuery() { if (this.session?.apiKey) return `apiKey=${this.session.apiKey}`; if (this.session?.token) return `token=${this.session.token}`; return "" }, init(e) { this.session = e, this.isInitialized = !0 }, activate() { w.loadCSS("./css/sections/profile.css"), this.setupUIInternal(), this.loadProfileData(), this.loadAnalytics(), this.startSmartPolling() }, deactivate() { this.rateLimitPollInterval && (clearInterval(this.rateLimitPollInterval), this.rateLimitPollInterval = null) }, startSmartPolling() { this.rateLimitPollInterval && clearInterval(this.rateLimitPollInterval); const e = localStorage.getItem("dashboard_last_sync"), t = Date.now(), s = 3e4; if (e) { const n = t - parseInt(e); n < s ? this.countdown = Math.ceil((s - n) / 1e3) : (this.countdown = 30, this.performSync()) } else this.countdown = 30, this.performSync(); this.updateSyncIndicator(), this.rateLimitPollInterval = setInterval(() => { typeof this.countdown == "number" && (this.countdown--, this.countdown <= 0 && (this.performSync(), this.countdown = 30)), this.updateSyncIndicator() }, 1e3) }, updateSyncIndicator() { const e = document.getElementById("profile-sync-indicator"); e && (e.textContent = `${this.countdown}s`) }, async performSync() { const e = document.getElementById("profile-sync-indicator"); this.session && (localStorage.setItem("dashboard_last_sync", Date.now().toString()), await Promise.all([this.pollRateLimit(), this.loadAnalytics()])), setTimeout(() => { e && e.classList.remove("syncing") }, 1e3) }, setupUIInternal() { if (!this.session) return; const e = document.getElementById("profile-user-id"), t = document.getElementById("profile-display-name"), s = document.getElementById("profile-large-avatar"); e && (e.textContent = this.session.userId || "---"), t && (t.textContent = this.session.displayName || this.session.login || "Streamer"), s && this.session.profile_image_url && (s.src = this.session.profile_image_url); const n = document.getElementById("profile-api-key"); if (n) { const a = this.session.apiKey || this.session.token || ""; n.value = a, n.dataset.realValue = a } this.setupTokenVisibility(), this.setupRegenerate(), this.setupCopyId(), this.setupDangerToggle(), this.setupDangerZone() }, setupDangerToggle() { const e = document.getElementById("profile-toggle-danger"), t = document.getElementById("danger-zone-section"); e && t && !e.dataset.listener && (e.addEventListener("click", () => { t.classList.contains("is-hidden") ? (t.classList.remove("is-hidden"), e.classList.add("active"), e.title = "Ocultar Zona de Peligro", setTimeout(() => { const n = window.pageYOffset, i = document.documentElement.scrollHeight - window.innerHeight - n, o = 1200; let l = null; const u = f((c, m, p, E) => p * ((c = c / E - 1) * c * c * c * c + 1) + m, "easeOutQuint"), d = f(c => { l === null && (l = c); const m = c - l, p = u(m, n, i, o); window.scrollTo(0, p), m < o ? requestAnimationFrame(d) : window.scrollTo(0, document.documentElement.scrollHeight) }, "animation"); requestAnimationFrame(d) }, 400)) : (t.classList.add("is-hidden"), e.classList.remove("active"), e.title = "Mostrar Zona de Peligro") }), e.dataset.listener = "true") }, async loadProfileData() { if (this.session) try { const q = this.authQuery ? `&${this.authQuery}` : "", e = `${g.API_ENDPOINTS.USER_INFO}?login=${this.session.login}${q}`, t = await fetch(e, { headers: this.authHeaders }); if (t.ok) { const s = await t.json(); this.updateProfileStatsInternal(s), this.updateBadgesInternal(s) } } catch (e) { console.error("[Profile] Error loading data:", e) } }, async pollRateLimit() { if (this.session) try { const q = this.authQuery ? `&${this.authQuery}` : "", e = `${g.API_ENDPOINTS.USER_INFO}?login=${this.session.login}${q}`, t = await fetch(e, { headers: this.authHeaders }); if (t.ok) { const s = await t.json(), n = document.getElementById("profile-stat-ratelimit"); n && s.rateLimit && (n.textContent = `${s.rateLimit} req/min`) } } catch { } }, async loadAnalytics() { if (this.session) try { const q = this.authQuery ? `?${this.authQuery}` : "", e = await fetch(`${g.API_ENDPOINTS.ANALYTICS}${q}`, { headers: this.authHeaders }); if (e.ok) { const t = await e.json(); this.renderCommandStatsInternal(t) } } catch (e) { console.error("Error updating statistics", e) } }, renderCommandStatsInternal(e) {
        const t = document.getElementById("profile-commands-grid"); if (!t) return; const s = [{ key: "clips", icon: "fa-film", label: "Clips" }, { key: "followage", icon: "fa-clock", label: "Followage" }, { key: "so", icon: "fa-bullhorn", label: "Shoutouts" }]; if (t.children.length === 0) {
            let n = ""; s.forEach(a => {
                n += `
                    <div class="stat-card">
                        <div class="stat-icon"><i class="fa-solid ${a.icon}"></i></div>
                        <div class="stat-info">
                            <h3 class="counter" id="prof-stat-${a.key}" data-target="0">0</h3>
                            <span>${a.label}</span>
                        </div>
                    </div>
                `}), t.innerHTML = n
        } s.forEach(n => { const a = document.getElementById(`prof-stat-${n.key}`), i = e[n.key] || 0; a && (this.lastData.analytics[n.key] !== i ? (r.animateValue(a, null, i), this.lastData.analytics[n.key] = i) : a.textContent = i.toLocaleString()) })
    }, updateProfileStatsInternal(e) { const t = document.getElementById("profile-stat-followers"), s = document.getElementById("profile-bio"), n = document.getElementById("profile-stat-broadcaster"), a = document.getElementById("profile-stat-created"); if (t) { const o = e.followers || 0; this.lastData.followers !== o ? (r.animateValue(t, 0, o, 1500), this.lastData.followers = o) : t.textContent = o.toLocaleString() } if (s && (s.textContent = e.description || "Sin biograf\xEDa disponible. \xA1Este streamer es un misterio!"), n) { const o = { partner: "Partner", affiliate: "Afiliado", "": "Streamer" }, l = e.broadcaster_type || ""; n.textContent = o[l] || "Streamer" } if (a && e.created_at) try { const o = new Date(e.created_at), l = { day: "2-digit", month: "short", year: "numeric" }; a.textContent = o.toLocaleDateString("es-ES", l) } catch { a.textContent = "---" } const i = document.getElementById("profile-stat-ratelimit"); i && e.rateLimit && (i.textContent = `${e.rateLimit} req/min`) }, updateBadgesInternal(e) { const t = document.getElementById("profile-badges-container"); if (!t) return; let s = ""; e.broadcaster_type === "partner" ? s += '<span class="profile-badge-status"><i class="fa-solid fa-check-circle"></i> Partner de Twitch</span>' : e.broadcaster_type === "affiliate" ? s += '<span class="profile-badge-status"><i class="fa-solid fa-star"></i> Afiliado de Twitch</span>' : s += '<span class="profile-badge-status secondary"><i class="fa-solid fa-user"></i> Streamer</span>', s += '<span class="profile-badge-status secondary"><i class="fa-solid fa-key"></i> LosPerris Access</span>', t.innerHTML = s }, setupTokenVisibility() { const e = document.getElementById("profile-toggle-key"), t = document.getElementById("profile-api-key"); e && t && !e.dataset.listener && (e.addEventListener("click", () => { t.type === "password" ? (t.type = "text", t.value = t.dataset.realValue || "", e.innerHTML = '<i class="fa-regular fa-eye-slash"></i>') : (t.type = "password", e.innerHTML = '<i class="fa-regular fa-eye"></i>') }), e.dataset.listener = "true") }, setupCopyId() { const e = document.getElementById("profile-copy-id-btn"); e && !e.dataset.listener && (e.addEventListener("click", () => { const s = document.getElementById("profile-user-id")?.textContent?.trim(); !s || s === "---" || navigator.clipboard.writeText(s).then(() => { const n = e.querySelector("i"); n && (n.className = "fa-solid fa-check", n.style.opacity = "1", n.style.color = "var(--success)", setTimeout(() => { n.className = "fa-regular fa-copy", n.style.opacity = "0.5", n.style.color = "" }, 1500)) }) }), e.dataset.listener = "true") }, setupRegenerate() { const e = document.getElementById("profile-regen-key"), t = document.getElementById("regen-modal"); e && !e.dataset.listener && (e.addEventListener("click", async () => { if (!t) return; if (!document.getElementById("confirm-regen-btn") && t.dataset.src) try { await h.load(t.dataset.src, t.id) } catch { r.showToast("Error al cargar modal de regeneraci\xF3n", "error"); return } const s = document.getElementById("confirm-regen-btn"); s && !s.dataset.listener && (s.addEventListener("click", async () => { const i = document.getElementById("close-regen-btn"); i ? i.click() : t.close(), r.setButtonLoading(e, !0); try { const l = await (await fetch(`${g.API_ENDPOINTS.REGENERATE_KEY}?userId=${this.session?.userId}`)).json(); if (l.apiKey && this.session) { this.session.apiKey = l.apiKey, (await import("./auth.js")).Auth.saveSession(this.session); const d = document.getElementById("profile-api-key"); d && (d.dataset.realValue = l.apiKey, d.type === "text" && (d.value = l.apiKey)), r.showToast(y.regenerateSuccess, "success") } } catch { r.showToast(y.regenerateError, "error") } finally { r.setButtonLoading(e, !1) } }), s.dataset.listener = "true"); const n = document.getElementById("close-regen-btn"), a = document.getElementById("cancel-regen-btn"); n && (n.onclick = () => t.close()), a && (a.onclick = () => t.close()), t.showModal() }), e.dataset.listener = "true") }, async openDangerModal(e) { const t = document.getElementById("danger-action-modal"); if (!t) { r.showToast("Error: Modal de seguridad no encontrado", "error"); return } if (!document.getElementById("danger-modal-title") && t.dataset.src) try { await h.load(t.dataset.src, t.id) } catch { r.showToast("Error al cargar componente de seguridad", "error"); return } const s = document.getElementById("danger-modal-title"), n = document.getElementById("danger-modal-desc"), a = document.getElementById("danger-modal-word"), i = document.getElementById("danger-modal-confirm"), o = document.getElementById("danger-modal-submit"), l = document.getElementById("danger-modal-close"), u = document.getElementById("danger-modal-cancel"); if (!s || !n || !a || !i || !o) { r.showToast("Error: Componentes del modal incompletos", "error"), console.error("[Profile] Missing modal elements:", { titleEl: s, descEl: n, wordEl: a, inputEl: i, submitBtn: o }); return } s.innerText = e.title, n.innerText = e.desc, a.innerText = e.word, i.value = "", o.disabled = !0, t.classList.remove("shake"); const d = f(() => { o.disabled = i.value.trim().toUpperCase() !== e.word }, "validate"); return i.oninput = d, new Promise(c => { const m = f(() => { i.oninput = null, l && (l.onclick = null), u && (u.onclick = null), o.onclick = null, t.open && t.close(), c() }, "cleanup"); o.onclick = async () => { if (i.value.trim().toUpperCase() === e.word) { r.setButtonLoading(o, !0); try { await e.onConfirm(), m() } catch { r.showToast("Error en la acci\xF3n confirmada", "error") } finally { r.setButtonLoading(o, !1) } } else t.classList.add("shake"), setTimeout(() => t.classList.remove("shake"), 500) }, l && (l.onclick = m), u && (u.onclick = m), t.showModal() }) }, setupDangerZone() { const e = document.getElementById("profile-clear-data-btn"), t = document.getElementById("profile-delete-account-btn"); e && !e.dataset.listener && (e.addEventListener("click", () => { this.openDangerModal({ title: "Reiniciar Estad\xEDsticas", desc: "Esta acci\xF3n borrar\xE1 todo el historial de comandos, clips y latencia. Tu cuenta y API Key seguir\xE1n activas.", word: "LIMPIAR", onConfirm: f(async () => { try { const q = this.authQuery ? `?${this.authQuery}` : ""; const n = await (await fetch(`${g.API_ENDPOINTS.CLEAR_DATA}${q}`, { method: "POST", headers: { "Content-Type": "application/json", ...this.authHeaders }, body: JSON.stringify({ confirm: "LIMPIAR" }) })).json(); n.success ? (r.showToast(n.message, "success"), setTimeout(() => window.location.reload(), 1500)) : r.showToast(n.error || "Error al limpiar datos", "error") } catch { r.showToast("Error de conexi\xF3n", "error") } }, "onConfirm") }) }), e.dataset.listener = "true"), t && !t.dataset.listener && (t.addEventListener("click", () => { this.openDangerModal({ title: "Eliminar Perfil de LosPerris API", desc: "\xA1ATENCI\xD3N! Esta acci\xF3n es irreversible dentro de nuestra plataforma. Se borrar\xE1n tus datos y API Key. Esto NO afectar\xE1 a tu canal ni cuenta de Twitch de ninguna manera.", word: "ELIMINAR", onConfirm: f(async () => { try { const q = this.authQuery ? `?${this.authQuery}` : ""; const n = await (await fetch(`${g.API_ENDPOINTS.DELETE_ACCOUNT}${q}`, { method: "DELETE", headers: { "Content-Type": "application/json", ...this.authHeaders }, body: JSON.stringify({ confirm: "ELIMINAR" }) })).json(); n.success ? (r.showToast("Cuenta eliminada. Redirigiendo...", "success"), setTimeout(() => { window.location.href = "/logout" }, 2e3)) : r.showToast(n.error || "Error al eliminar cuenta", "error") } catch { r.showToast("Error de conexi\xF3n", "error") } }, "onConfirm") }) }), t.dataset.listener = "true") }
}; export { C as ProfileModule };
