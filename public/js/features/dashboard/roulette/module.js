import { UI as c } from "../../../core/ui.js"; import { Messages as y } from "../../../shared/i18n/messages.js"; import { RouletteMessages as d } from "./messages.js"; import { DASHBOARD_CONFIG as v } from "../dashboard-config.js"; const { API_ENDPOINTS: E, IGNORED_BOTS: w } = v; import { TmiService as u } from "../../../services/tmiService.js"; const k = { session: null, chatters: [], canvas: null, ctx: null, colors: ["#a855f7", "#3b82f6", "#10b981", "#f59e0b", "#ef4444", "#ec4899", "#6366f1"], startAngle: 0, arc: 0, spinTimeout: null, spinAngleStart: 10, spinTime: 0, spinTimeTotal: 0, isSpinning: !1, isOpen: !1, isConnected: !1, isInitialized: !1, cssLoaded: !1, get authHeaders() { const t = { "Content-Type": "application/json" }; return this.session?.token && (t.Authorization = `Bearer ${this.session.token}`), t }, get authQuery() { if (this.session?.apiKey) return `apiKey=${encodeURIComponent(this.session.apiKey)}`; if (this.session?.token) return `token=${encodeURIComponent(this.session.token)}`; return "" }, init(t) { this.session = t, this.cssLoaded || (import("../../../shared/utils/loader.js").then(({ Loader: e }) => { e.loadCSS("css/sections/roulette.css") }), this.cssLoaded = !0), this.setupUI(), this.updateUI(), this.isInitialized = !0 }, deactivate() { this.isOpen = !1, this.spinTimeout && cancelAnimationFrame(this.spinTimeout), u.removeListener("roulette"), u.disconnect(), this.isConnected = !1, this.isSpinning = !1 }, setupUI() { if (this.canvas = document.getElementById("roulette-canvas"), !this.canvas) return; this.ctx = this.canvas.getContext("2d"); const t = document.getElementById("btn-spin-roulette"), e = document.getElementById("toggle-roulette"), s = document.getElementById("btn-refresh-roulette"), i = document.getElementById("close-winner-display"); t && !t.dataset.listener && (t.addEventListener("click", () => this.spin()), t.dataset.listener = "true"), e && !e.dataset.listener && (e.addEventListener("click", () => this.toggleEntries()), e.dataset.listener = "true"), s && !s.dataset.listener && (s.addEventListener("click", () => { this.loadChatters(), c.showToast(d.updatedRaw, "success", "fa-check") }), s.dataset.listener = "true"), i && !i.dataset.listener && (i.addEventListener("click", () => { document.getElementById("roulette-winner-display")?.classList.add("hidden") }), i.dataset.listener = "true"), this.drawEmptyWheel() }, toggleEntries() { this.isOpen = !this.isOpen; const t = document.getElementById("toggle-roulette"); t && (t.className = this.isOpen ? "btn-icon btn-warning" : "btn-icon btn-success", t.innerHTML = this.isOpen ? '<i class="fa-solid fa-pause"></i>' : '<i class="fa-solid fa-play"></i>'), this.isOpen ? (c.showToast(d.openRaw, "success", "fa-door-open"), this.loadChatters(), this.connectTmi()) : (c.showToast(d.closedRaw, "warning", "fa-door-closed"), u.disconnect(), this.isConnected = !1) }, async connectTmi() { if (this.isConnected || !this.session) return; const t = this.session.token ? { username: this.session.login, token: this.session.token } : void 0; try { await u.connect(this.session.login, t), this.isConnected = !0, u.addListener("roulette", (e, s, i) => { if (this.isSpinning || !this.isOpen) return; const n = s.username; w.has(n.toLowerCase()) || this.chatters.some(a => a.user_login.toLowerCase() === n.toLowerCase()) || (this.chatters.push({ user_login: n, user_name: s["display-name"] || n }), this.updateUI(), this.pulseCounter()) }) } catch (e) { console.error("Roulette TMI Error:", e), c.showToast(y.Common.connectionError || "Error connecting to chat", "error"), this.toggleEntries() } }, pulseCounter() { const t = document.getElementById("roulette-count"); t && (t.classList.add("count-pulse"), setTimeout(() => t.classList.remove("count-pulse"), 500)) }, updateUI() { const t = document.getElementById("roulette-count"); t && (t.textContent = String(this.chatters.length)), this.drawRouletteWheel() }, loadChatters() { if (!this.session) return; const { login: s, displayName: i } = this.session, n = new Set(this.chatters.map(o => o.user_login)); let a = 0; n.has(s) || (this.chatters.push({ user_login: s, user_name: i || s }), n.add(s), a++), a > 0 && this.updateUI(); const q = this.authQuery ? `&${this.authQuery}` : ""; fetch(`${E.CHATTERS}?channel=${s}${q}`, { headers: this.authHeaders }).then(o => o.json()).then(o => { const l = o, f = Array.isArray(l) ? l : l.chatters || []; if (Array.isArray(f)) { const g = new Set(this.chatters.map(r => r.user_login.toLowerCase())); let x = 0; f.forEach(r => { const m = typeof r == "string" ? r : r.user_login, T = typeof r == "string" ? r : r.user_name; if (!m) return; const p = m.toLowerCase(); !g.has(p) && !w.has(p) && (this.chatters.push({ user_login: m, user_name: T }), g.add(p), x++) }), x > 0 && (this.updateUI(), this.pulseCounter()) } }).catch(o => { console.error("Error loading chatters:", o), c.showToast("Error al cargar usuarios del chat", "error") }) }, spin() { this.isSpinning || this.chatters.length === 0 || (this.isSpinning = !0, this.spinAngleStart = Math.random() * 10 + 10, this.spinTime = 0, this.spinTimeTotal = Math.random() * 3e3 + 4e3, this.rotateWheel()) }, rotateWheel() { if (this.spinTime += 20, this.spinTime >= this.spinTimeTotal) { this.stopRotateWheel(); return } const t = this.spinAngleStart - this.easeOut(this.spinTime, 0, this.spinAngleStart, this.spinTimeTotal); this.startAngle += t * Math.PI / 180, this.drawRouletteWheel(), this.spinTimeout = requestAnimationFrame(() => this.rotateWheel()) }, stopRotateWheel() { this.spinTimeout && cancelAnimationFrame(this.spinTimeout), this.isSpinning = !1; const t = this.startAngle * 180 / Math.PI % 360, e = 360 / this.chatters.length, s = Math.floor((360 - (t + 90) % 360) % 360 / e), i = this.chatters[s % this.chatters.length]; this.showWinner(i) }, easeOut(t, e, s, i) { const n = (t /= i) * t, a = n * t; return e + s * (a + -3 * n + 3 * t) }, showWinner(t) { const e = document.getElementById("roulette-winner-display"), s = document.getElementById("winner-name"); if (e && s) { const i = this.chatters.length, n = c.escapeHTML(t.user_name); s.innerHTML = d.winner(n, i), e.classList.remove("hidden"), c.showToast(d.winner(n, i), "success", "fa-trophy"); if (this.session) { const q = this.authQuery ? `?${this.authQuery}` : ""; fetch(`${E.SEND_MESSAGE}${q}`, { method: "POST", headers: this.authHeaders, body: JSON.stringify({ message: `\u{1F3C6} \xA1El ganador es @${t.user_name}! (De ${i} participantes) \xA1Felicidades! \u{1F389}` }) }).catch(o => console.error("Error sending winner message:", o)) } } }, drawRouletteWheel() { if (!this.canvas || !this.ctx) return; const t = 200, e = 160, s = 50; this.ctx.clearRect(0, 0, 500, 500); const i = this.chatters.length; if (i === 0) { this.drawEmptyWheel(); return } this.arc = Math.PI * 2 / i; const n = this.canvas.width / 2, a = this.canvas.height / 2; for (let h = 0; h < i; h++) { const o = this.startAngle + h * this.arc; this.ctx.fillStyle = this.colors[h % this.colors.length], this.ctx.beginPath(), this.ctx.arc(n, a, t, o, o + this.arc, !1), this.ctx.arc(n, a, s, o + this.arc, o, !0), this.ctx.stroke(), this.ctx.fill(), this.ctx.save(), this.ctx.shadowOffsetX = -1, this.ctx.shadowOffsetY = -1, this.ctx.shadowBlur = 0, this.ctx.fillStyle = "white", this.ctx.font = "bold 14px Poppins, sans-serif", this.ctx.translate(n + Math.cos(o + this.arc / 2) * e, a + Math.sin(o + this.arc / 2) * e), this.ctx.rotate(o + this.arc / 2 + Math.PI / 2); const l = this.chatters[h].user_name; this.ctx.fillText(l, -this.ctx.measureText(l).width / 2, 0), this.ctx.restore() } }, drawEmptyWheel() { if (!this.canvas || !this.ctx) return; const t = this.canvas.width / 2, e = this.canvas.height / 2; this.ctx.clearRect(0, 0, 500, 500), this.ctx.beginPath(), this.ctx.arc(t, e, 200, 0, 2 * Math.PI), this.ctx.fillStyle = "#1a1625", this.ctx.fill(), this.ctx.lineWidth = 3, this.ctx.strokeStyle = "#9146ff", this.ctx.stroke(), this.ctx.fillStyle = "#ffffff", this.ctx.font = "bold 22px Poppins, sans-serif", this.ctx.textAlign = "center", this.ctx.shadowBlur = 10, this.ctx.shadowColor = "#9146ff", this.ctx.fillText("Esperando", t, e - 10), this.ctx.fillText("Participantes...", t, e + 22), this.ctx.shadowBlur = 0 } }; export { k as RouletteModule };
